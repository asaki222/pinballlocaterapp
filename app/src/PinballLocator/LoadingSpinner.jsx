import React from 'react';
import { Spinner, SpinnerContainer } from '../Styles/StyledErrorLoading';

const LoadingSpinner = () => {
  return  (
  <SpinnerContainer>
  <Spinner />
</SpinnerContainer>
  )
};

export default LoadingSpinner;
